---
- name: Configure nginx server
  hosts: webserver
  tasks:
    - name: Install nginx
      apt:
        name: nginx=1.24*
        state: present

    - name: Start nginx service
      service:
        name: nginx
        state: started